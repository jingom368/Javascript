<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Enter</title>
    </head>
    <body>
        <%- include('menu.html') %>
        <!-- form>div>label{제목}+input[type="text"][name="title"]^p+div>label{내용}+textarea[name="content"]^p+button[type="submit"]     -->
        <form action="/edit" method="post">
            <div class="form-group">
                <label>제목</label>
                <input
                    type="text"
                    name="id"
                    value="<%= data._id %>"
                    style="display: none"
                />
                <input
                    type="text"
                    name="title"
                    value="<%= data.title %>"
                    class="form-control"
                />
            </div>
            <p></p>
            <div class="form-group">
                <label for="">내용</label>
                <textarea name="content" rows="10" class="form-control">

                    <%= data.content %></textarea
                >
            </div>
            <div class="form-group">
                <label>작성일</label>
                <input
                    type="date"
                    class="form-control"
                    name="someDate"
                    value="<%= data.date %>"
                />
            </div>
            <p></p>
            <button type="submit">저장</button>
        </form>
    </body>
</html>
